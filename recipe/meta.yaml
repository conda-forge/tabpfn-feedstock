{% set name = "tabpfn" %}
{% set version = "2.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/tabpfn-{{ version }}.tar.gz
  sha256: 8a29f8b0a8844855afec7920dae8c120a3f7dd71a1eccbca1e8e7c02a5be73de

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.7
    - hatchling
    - pip
  run:
    - python >=3.7
    - pytorch >=1.9.0
    - scikit-learn >=0.24.2
    - pyyaml >=5.4.1
    - numpy >=1.21.2
    - requests >=2.23.0

test:
  imports:
    - tabpfn
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/automl/TabPFN
  summary: Interface for using TabPFN and library to train TabPFN'
  license: Apache-2.0
  license_file:
    - LICENSE.txt
    - NOTICE.txt

extra:
  recipe-maintainers:
    - anuprulez
